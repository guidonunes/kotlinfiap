package com.example.kotlinfiap.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)


// CORES DO TEMA CLARO
// CORES DO TEMA CLARO
val LightPrimary = Color(0xFF0A58D6)   // Clean, electric tech blue
val LightSecondary = Color(0xFF5E6E82) // Cool steel gray for secondary elements
val LightBackground = Color(0xFFF4F7FA) // Soft, icy off-white (reduces eye strain)
val LightSurface = Color(0xFFFFFFFF)   // Pure white for cards to pop against the background
val LightOnPrimary = Color(0xFFFFFFFF) // Crisp white text on blue
val LightOnSecondary = Color(0xFFFFFFFF) // White text on steel gray
val LightOnBackground = Color(0xFF1A202C) // Deep slate instead of harsh black
val LightOnSurface = Color(0xFF1A202C)    // Deep slate text on cards
val LightTertiary = Color(0xFF00BFA5)  // Vibrant cyan/teal accent for techy highlights
val LightOnTertiary = Color(0xFF000000) // Dark text on cyan for contrast

// CORES DO TEMA ESCURO
val DarkPrimary = Color(0xFF0070D1)   // Vibrant PlayStation Blue
val DarkSecondary = Color(0xFF757575) // Sleek Controller Slate Gray
val DarkBackground = Color(0xFF0D0D12) // Deep console black with a tiny hint of blue
val DarkSurface = Color(0xFF1A1A24)   // Slightly elevated card color to separate from background
val DarkOnPrimary = Color(0xFFFFFFFF) // Crisp white text on blue
val DarkOnSecondary = Color(0xFFFFFFFF) // White text on gray
val DarkOnBackground = Color(0xFFF5F5F5) // Soft white text to reduce eye strain
val DarkOnSurface = Color(0xFFF5F5F5)    // Soft white text on cards
val DarkTertiary = Color(0xFFDF005E)  // Magenta/Pink accent (inspired by the Square button)
val DarkOnTertiary = Color(0xFFFFFFFF) // White text on magenta